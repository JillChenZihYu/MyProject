<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - 訂位機器人</title>

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.8.3.js"></script>
    @*<link href="~/Content/Site.css" rel="stylesheet" type="text/css" />*@
    <link href="~/Content/MyStyle.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
</head>
<body class="background">
    <div class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            @Html.ActionLink("訂位機器人", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#MynavbarMenu">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="MynavbarMenu">
                <div class="navbar-nav">
                    @Html.ActionLink("首頁", "Index", "Home", new { area = "" }, new { @class = "nav-link bi bi-house-fill mr-2" })
                    @Html.ActionLink("會員登入", "Login", "Home", new { area = "" }, new { @class = "nav-link bi bi-person-fill mr-2" })
                    @Html.ActionLink("註冊會員", "Create", "Members", new { area = "" }, new { @class = "nav-link bi bi-person-plus-fill mr-2" })
                    @Html.ActionLink("管理員登入", "Login", "HomeManager", new { area = "" }, new { @class = "nav-link bi bi-wrench-adjustable mr-2" })
                </div>
            </div>
        </div>
    </div>
    <div>
        <div>
            <div class="container body-content">
                @RenderBody()
                <hr />

            </div>
            <div class="container">
                <footer>
                    <p>&copy; @DateTime.Now.Year - 我的 ASP.NET 應用程式</p>

                </footer>
            </div>
        </div>
    </div>
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    @RenderSection("scripts", false)   @*渲染HomeManager裡Login的section，true為必要每個區塊都渲染，false為非必要，有需要再渲染*@
 
</body>
</html>
